#!/usr/bin/perl
 
use strict;
use warnings;

### input ###
# config main config from App
# param main parameter
# solution from create_sol
#
### output ###
# s* best solution for this config

sub meta_algo {
	# set parameter

	# iterator over params (it max, pop_max)

	# create set of solutions (n)
	#   iterrate over solutions (j_ni)
	#     eval function objective
	#   select best solution n1 < n
	# iterate
}
1
