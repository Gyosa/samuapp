package Mmas::MainAlgo;
use strict;
use warnings;
#use diagnostics;

use lib '../Metaheu';

use DateTime;
use Data::Dumper;

sub run {
    my ($params, $config) = @_;

    # while nb expérience or delais is over

      # for each fourmis

        # for each creneau and nb personnes

        
}
1;
